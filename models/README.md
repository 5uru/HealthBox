# Models

This directory contains various files related to the training of different models used in the project. Each model is trained using specific datasets and configurations to achieve optimal performance for the intended tasks.